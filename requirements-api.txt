# FastAPI backend dependencies
fastapi>=0.115.0,<1.0
uvicorn[standard]>=0.34.0,<1.0
pydantic>=2.0,<3.0
python-dotenv>=1.0
python-multipart>=0.0.7

# Data & computation
pandas>=2.0,<3.0
numpy>=2.0,<3.0
scipy>=1.12,<2.0
openpyxl>=3.1,<4.0

# HTTP
requests>=2.31,<3.0
urllib3>=2.0,<3.0

# Market data
akshare>=1.14
yfinance>=1.2,<2.0

# V8 engine for akshare
# NOTE: use `mini-racer` (the maintained fork, 0.14+) — NOT `py-mini-racer` (legacy
# 0.6.0, 2020). Both install into the py_mini_racer/ namespace; if both get
# installed the legacy __init__.py wins and calls old symbols on the new dylib,
# crashing with `dlsym: mr_eval_context symbol not found`. akshare pulls
# mini-racer in transitively but we pin explicitly for reproducibility.
mini-racer

# Authentication
bcrypt>=4.0,<5.0
PyJWT>=2.8,<3.0
mcp>=1.9,<2.0
